image: registry.gitlab.com/mpapp-private/manuscripts-frontend/node8-aws

stages:
  - build

build:
  stage: build
  script:
    - yarn install
    - yarn run build
    - yarn run coverage
  artifacts:
    paths:
      - ./coverage
    expire_in: 1 week
    when: always
